#ifndef IMPLEMENTATION_FACTORY_HPP
#define IMPLEMENTATION_FACTORY_HPP

// definicja szablonu klasy NodeCollection
#include "nodes.hpp"
#include "storage_types.hpp"

// using container_t = typename IPackageQueue::const_iterator<Node>;
// using iterator = typename container_t::iterator;
// using const_iterator = typename container_t::const_iterator;

// // definicja klasy Factory

// class IPackageReceiver
// {
//     ReceiverType get_receiver_type();
// };

// class Node
// /*

// */

// {
// };

// class NodeCollection
// {
// };

// class Factory
// /*
// Grupuje węzły sieci - rampy, robotników i magazyny. Oraz operacje na nich.
// */
// {
//     void add_ramp(Ramp &&r);
//     void remove_ramp(ElementID id);
//     NodeCollection<Ramp> find_ramp_by_id(ElementID id);
//     is_consistent(); // Czy każda jednostka półproduktu rozładowana na obojętnie z której ramp, zostanie ostatecznie umieszczona w magazynie.
//     do_deliveries();
//     do_package_passing();
//     do_work();
// };

#endif // IMPLEMENTATION_FACTORY_HPP
